1. 执行打包   npm run dist
2. 切换npm库(注意，url最后的"/"不能丢)  npm config set registry http://registry.npmjs.org
3. 登录npm(haomashroom//*******,442359694@qq.com)    npm login
4. 复制package.json 到 dist目录
5. 发布到npm库 npm publish dist
使用
1. 切换npm库(注意，url最后的"/"不能丢)    npm config set registry http://registry.npmjs.org
2. 安装库       npm install iMeTisUI  --save
3. 切回npm库 npm config set registry http://registry.npmjs.org
